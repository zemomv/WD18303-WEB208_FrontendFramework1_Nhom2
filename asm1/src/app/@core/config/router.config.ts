import {IRouterConfig} from "../interfaces";

export const ROUTER_CONFIG: IRouterConfig = {
  pageNotFound: 'not-found',
  forbidden: '403',
  auth: {
    login: 'auth/login',
  },
  pages: 'pages',
};
